apiVersion: external-secrets.io/v1beta1
kind: SecretStore
metadata:
  name: secret-store
  namespace: backstage
spec:
  provider:
    azurekv:
      authType: WorkloadIdentity
      vaultUrl: "vault_url"
      serviceAccountRef:
        name: backstage-sa
